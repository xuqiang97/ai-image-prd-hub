# AGENTS.md

本文件用于指导 ChatGPT、Codex 及其他 AI 在本仓库中新增和维护 AI 生图轻量需求 PRD。

## 1. 唯一文档原则

每个需求最终只维护一份正式 PRD：

```text
prd/YYYY-MM/需求目录/README.md
```

该 README.md 同时用于 GitHub 沉淀、研发实施、测试验收、钉钉流程和后续 AI 复用，是该需求唯一正式的范围、规则和验收来源。

不得额外生成钉钉版、GitHub 实施版、精简版、月份汇总版或其他重复文档。

特殊情况下，内容本身不构成独立需求，但属于某个需求的长期复用资料，且继续放入 README.md 会明显影响主 PRD 可读性时，经用户明确确认后，可以在该需求目录的 `docs/` 下创建下级辅助文档。辅助文档不是第二份 PRD，不单独维护禅道、PRD 版本、修改纪要、调研纪要、验收标准或根目录索引；本期新增或变更的范围、规则和验收仍以 README.md 为准。

## 2. 仓库定位

本仓库只收录**不需要单独制作 HTML 原型**的 AI 生图轻量需求。

判断标准：

> 本需求是否需要先通过页面原型确认交互和展示效果。

需要原型时，应单独建立原型仓库，通过 `HTML 原型 + 唯一 README.md PRD` 交付。

适合本仓库的需求包括：取数逻辑、接口字段、已上线接口的兼容扩展、配置规则、状态流转、localStorage、本地缓存、数据同步、后端处理口径、定时任务、线上修复和页面无明显变化的轻量前端逻辑。

## 3. 新需求处理流程

1. 判断是否需要 HTML 原型。
2. 梳理需求边界、是否拆分和关键缺失信息。
3. 判断更新历史需求还是新建独立需求。
4. 在聊天中完成需求澄清、实现口径和验收标准确认。
5. 等用户提供正式需求名称、禅道编号和禅道链接。
6. 信息齐全且用户明确“可以落地”后，在本地创建 `M-禅道号-英文短名` 正式目录。
7. 在本地生成或更新该需求唯一的 README.md。
8. 新建需求时，在本地更新根目录 README.md 的唯一需求索引。
9. 主动完成全盘复核，并向用户提供可直接打开的本地 PRD，等待用户检查和补充修改。
10. 用户明确确认“可以发布”或“提交发布 GitHub”后，再提交、创建 PR、合并，并验证本地与线上内容一致。

默认规则：

- 正式需求名称、禅道编号和链接齐全，且用户明确“可以落地”前，只继续讨论，不创建或修改正式需求文件。
- “可以落地”默认只授权创建或修改本地文档，不等同于授权提交或发布 GitHub。
- 本地 PRD 完成后必须先提供给用户复核；只有用户明确“可以发布”“提交发布 GitHub”或“直接发布、无需再确认”时，才执行 GitHub 写入。
- 已发布 PRD 的后续优化同样先停留在本地，完成复核并再次获得发布确认后再更新 GitHub。
- 发布后先核对本地 `main`、`origin/main` 与 GitHub 合并提交一致，再验证线上文件内容。Windows 工作区可能因 LF/CRLF 转换导致本地文件原始 SHA256 与 GitHub 下载文件不同；需要精确核对时应比较 Git blob SHA，并结合工作区无差异确认，不得仅凭原始文件哈希差异误判内容不一致。
- 默认不创建 `TEMP` 临时目录，避免后续删除、迁移和重命名。
- 仅在用户明确要求提前落库时创建临时目录，并说明后续需要正式化。
- 不创建月份 README，不维护第二份索引。
- 默认不创建下级辅助文档；只有用户明确确认其不构成独立需求、说明文档定位并授权本地落地后，才可在对应需求的 `docs/` 下创建。

## 4. 更新原需求还是新建需求

### 4.1 更新原 README.md

以下情况更新原需求，不新建目录：

- 同一禅道需求评审前继续补充或修正规则。
- 研发、测试提出澄清，但仍属于同一实施范围。
- 修改内容没有形成独立开发、测试或上线对象。

更新时保留原目录和禅道编号，仅修改发生变化的内容，不得无依据改写已确认口径。

### 4.2 新建 README.md

以下情况新建独立需求：

- 已有新的独立禅道需求。
- 原需求已上线，本次属于后续迭代。
- 新增内容可以独立开发、测试或上线。
- 本次存在新的需求边界和验收标准。
- 线上问题被单独转为修复需求。

不得把多个可独立实施的需求长期叠加在同一 README.md 中。

同一批次、同一页面或共享组件内的轻量顺带优化，如果业务目标相关、由同一批研发实现、无需独立测试或上线，可以作为同一需求的次要范围；但必须在需求概述、影响入口和验收标准中单独写清。能够独立开发、测试或上线，或具有独立业务目标时，仍应拆分为独立需求。

## 5. 目录规则

```text
prd/YYYY-MM/M-禅道号-英文短名/README.md
```

如经确认需要下级辅助文档，使用：

```text
prd/YYYY-MM/M-禅道号-英文短名/
├── README.md
└── docs/
    └── 英文短名.md
```

示例：

```text
prd/2026-07/M-36422-model-quantity-cache/README.md
prd/2026-06/M-35944-asin-query-logic/README.md
```

英文短名使用小写字母和中划线，建议 3～6 个单词，优先描述需求核心，不使用无意义的 `update`、`change`、`optimize` 单独命名。

下级辅助文档同样使用小写字母和中划线命名，名称应描述资料主题，不使用 `latest`、`new`、`最新版`、`新建文档` 等不稳定命名。不得把辅助文档直接散放在需求 README.md 旁边；统一放入 `docs/`，由父级 README.md 建立链接和说明。

## 6. README.md PRD 写作规则

### 6.1 基础结构

PRD 前部一般包括：

1. 文档标题
2. 禅道需求完整 URL
3. GitHub PRD 完整 URL
4. 修改纪要
5. 调研纪要
6. 业务背景和价值
7. 需求概述
8. 功能需求
9. 风险描述
10. 验收标准

标题层级：

- 需求总标题使用一级标题 `#`。
- 主要章节使用二级标题 `##`。
- 章节下的模块和规则使用三级标题 `###`。
- 不使用加粗正文模拟标题。

钉钉兼容要求：

- 每份需求 README.md 及其需要复制到钉钉的下级辅助 Markdown 文档，都必须以钉钉稳定兼容为最高优先级。
- 只使用一级至三级标题、普通段落、简单列表、简单表格和必要的代码块等已验证基础格式。
- 不使用引用块、提示块、HTML、复杂嵌套列表，或将标题、加粗和链接多层组合的装饰性样式；重点信息优先使用普通三级标题和直白文案表达。
- 每份会复制到钉钉的 Markdown 文档必须在开头直接展示自身的完整 GitHub URL，便于钉钉无法完整呈现 Mermaid 文本绘图、代码块或其他 Markdown 内容时回到 GitHub 查看原文。
- 需要从钉钉直接打开的 GitHub PRD、辅助文档或其他仓库资料，必须单独展示完整 `https://github.com/...` URL，不得只使用仓库内相对链接，也不得仅把地址隐藏在 Markdown 链接文字中。
- 本地交付用户复核时，应同时检查复制到钉钉后的标题、列表、表格、代码块和链接可用性；发现钉钉兼容问题后，先修正并重新获得用户发布确认，不沿用修正前的发布授权直接发布。

禅道需求和 GitHub PRD 必须直接展示完整 URL，不使用 `[文字](URL)` 形式隐藏地址。GitHub PRD 放在禅道需求下一行，方便复制到钉钉文档后直接查看和回跳原文。

推荐起始结构：

```text
# M-xxxxx【AI生图】[运营/美工/美工+运营/公共]需求名称（责任人：徐强）

禅道需求：https://pm.zhcxkj.com/zentao/story-view-xxxxx.html

GitHub PRD：https://github.com/xuqiang97/ai-image-prd-hub/blob/main/prd/YYYY-MM/M-xxxxx-英文短名/README.md

## 修改纪要

| 日期 | 版本 | 修改人 | 主要修改点 |
|---|---|---|---|
| YYYY-MM-DD | V1.0 | 徐强 | 新建 PRD，明确本次需求范围、核心规则和验收标准。 |

## 调研纪要

| 日期 | 业务部门 | 角色 | 调研对象（干系人） | 调研内容和结论 |
|---|---|---|---|---|
| YYYY-MM-DD | 暂无 | 暂无 | 暂无 | 暂无 |

## 业务背景和价值

## 需求概述

## 功能需求

### 1. 具体模块或规则

## 风险描述

## 验收标准
```

### 6.2 轻量与聚焦原则

轻量需求应**简明扼要、突出本期实现内容**，但不得为了缩短篇幅丢失会影响产品判断、研发实现或测试验收的必要规则。

写作时优先聚焦：

- 业务为什么提出该需求。
- 本期具体修改哪些入口、模块和逻辑。
- 核心业务规则、处理优先级和成功条件。
- 必要的异常处理、边界条件和兼容规则。
- 可以直接执行和验证的验收标准。

组织内容时：

- 同一规则只在最合适的章节完整说明，避免在功能需求、异常和边界、风险描述、验收标准中反复展开。
- “不在本期范围”只保留容易被研发、测试误解，或确实会影响实施边界的事项，不为形式完整罗列大量无关内容。
- 异常和边界优先并入对应功能规则；只有跨多个模块、无法归入具体功能的异常才单独成章。
- 验收标准用于验证功能规则，可以合并相近场景、适当控制篇幅，不机械复制前文全部内容。
- 业务背景优先描述当前状态、业务影响和本期目标。除非问题根因会直接影响修复范围或验收口径，否则不使用“遗漏”“漏做”“做错”等带有归责意味的历史表述，也不写与本期实现无关的人员或团队更替。
- PRD 正文只记录当前最终有效的业务状态、入口和实现口径，不记录内部讨论、历史文档纰漏、纠错过程或从未发生的错误实现；最终规则已经正向列明时，不再增加针对旧误写的反向解释。
- 只有历史错误已实际影响线上行为、兼容范围、数据修复、风险或验收时，才作为现状和修复边界保留，不得把单纯的文档更正写成功能规则或验收要求。
- 不适用章节直接删除，不机械套模板。
- 明确区分已确认前提、上线条件和未关闭风险：已由业务、上下游或现网验证确认的事实，直接写入业务背景或功能规则；确定必须满足的发布顺序、检查项写入对应功能规则或验收标准；只有仍有发生可能且会影响实现、上线或验收的事项才保留在风险描述中，已关闭事项不重复作为风险。

验收标准必须覆盖：

- 核心正常链路。
- 关键失败场景。
- 业务端隔离和账号隔离。
- 历史兼容和配置变化。
- 容易引发实现偏差的特殊场景。

避免模糊表达，如“尽量”“可能”“视情况”“正常处理”“相关逻辑”“保持一致”，应改为可执行、可测试的明确规则。

### 6.3 修改纪要

- 同一需求在正式提交研发、测试评审前，无论经历多少轮内部讨论、自查或优化，也无论是否已经写入 GitHub，均继续维护 `V1.0`；修改纪要不记录 AI 与用户的内部讨论过程，GitHub 提交次数不等同于文档版本升级。
- 正式提交评审或形成对外基线后，发生影响需求范围、业务规则、接口口径或验收标准的实质变化时，才增加 `V1.1`、`V1.2`。
- 仅修正文案、格式、错别字或压缩重复内容，且不影响研发实现和测试验收时，不机械新增版本。

### 6.4 调研纪要

- 调研纪要原则上记录真实业务调研和业务干系人结论，优先写明业务部门、使用角色、实际问题和确认口径。
- 业务方提出的问题、实际使用反馈和确认口径均可作为调研纪要。
- 研发、测试、产品内部的口头同步、任务交接或“已经告知研发”等信息，不作为业务调研纪要。
- 研发或测试提出的技术可行性、接口约束、实现风险等结论，如确实影响产品决策，应写入对应功能规则、风险描述或上线协作说明，不冒充业务调研。
- 已有美工、运营等业务人员的真实反馈时，不得仍填写“暂无”。
- 没有调研信息时才写“暂无”，不得虚构。

M-36422 调研结论参考表达：

> 美工和运营业务反馈，用户实际使用时通常不会每次都按系统允许的最大张数生图，导致新增任务或批量导入时需要反复调整“模型与数量配置”，因此希望系统记忆用户个人上一次成功使用的配置。

### 6.5 初稿后的主动复核

AI 完成 PRD 初稿后，必须主动进行一次全盘复核，再交付最终稿。重点检查：

- 是否存在规则冲突或前后矛盾。
- 是否遗漏真实业务入口；同一组件被多个页面、路由或业务端复用时，是否逐一列明实际入口，而不是只写“相关页面”“对应入口”。
- 是否区分直接改造点与回归范围；共享后端或共享组件统一修改时，应列明全部实际入口用于回归，但不得误写为各页面均需分别开发。
- 主需求之外的轻量顺带优化，是否也在需求概述、影响范围和验收标准中明确体现。
- 是否存在研发可能产生不同理解的歧义。
- 是否准确区分现有字段与新增字段、请求入参与响应出参，以及名称相近的单数/复数字段。
- 在现有搜索框或筛选组件中增加字段时，是否只定义了新增字段的匹配规则而误改原有字段口径；规则表、示例和验收标准对精确、模糊、大小写及空格处理的描述是否前后一致。
- 已上线接口是否覆盖现网调用兼容、独立发布、下游沟通和接口文档更新。
- 最终复核若提出新的状态门槛、触发条件、重试机制、覆盖优先级或历史补数能力，是否已重新获得用户确认；不得将未确认的业务规则以“优化”名义直接写入 PRD。
- 修改纪要、调研纪要和根目录索引日期是否一致，并使用需求正式创建或落库日期；跨日期协作时必须重新核对。
- 是否为了严谨而写得过重。
- 是否可以通过合并重复内容提升可读性。
- 是否残留内部讨论、文档纠错过程、从未发生的错误实现或仅用于解释旧误写的反向表述。
- 验收标准是否覆盖核心链路、失败场景、隔离、兼容和特殊场景；同一能力由多个业务端、页面或独立接口分别实现时，是否明确要求各入口分别执行对应验收，而不是只用“保持一致”代替实际覆盖。

最终目标是形成一份**篇幅适中、重点突出、口径明确、研发可执行、测试可验收**的唯一正式 README.md PRD。

### 6.6 外部 AI 评分与评审结果复核

- 外部 AI 评分不是每个需求的默认步骤或交付门槛；仅在用户提供评分报告或明确要求评分复核时处理，不得为了获取分数主动改变正常的需求梳理和交付流程。
- 外部 AI 评分、自动评审和检查报告只作为发现问题的线索，不作为修改 PRD 或追求分数的直接依据。收到报告后必须回到 PRD 原文、实际业务资料和本仓库规则逐项核对。
- 先区分真实遗漏或歧义、可提升可读性的建议、表格或链接解析失真、通用模板偏好，以及会扩大本期范围的未确认建议；不得把评分结论直接当作已确认业务规则。
- 报告声称章节为空、字段缺失或验收不完整时，先检查对应内容是否已通过表格、列表或关联章节表达，并核对评分工具是否遗漏了这些结构；不得为了便于工具识别而机械复制同一规则或新增无意义章节。
- 只有能够提升产品判断、研发实施或测试验收准确性，且有业务确认、现网事实或可靠资料支撑的建议，才写入 PRD。并发、幂等、重试、降级、回滚、监控、开关、弹窗等通用建议，如不属于本期真实问题，不得为了评分补写。
- 对方向合理但依赖现网行为、接口差异或业务选择的建议，先转为核对项或确认问题；没有事实依据前不得自行补全。复核后不存在实质缺口时，保持原文不变是有效结论，不因分数高低机械调整文档或升级版本。

### 6.7 通用经验沉淀

- 需求完成后，只将能够跨需求稳定复用的仓库流程、文档职责、写作规则和复核标准沉淀到根目录 `README.md` 或 `AGENTS.md`。
- 单个需求的业务数值、专属文案、页面入口、状态流转、上下游特殊约束和验收样例，只保留在该需求目录中，不上升为仓库通用规则；本期正式结论写入 README.md，经确认的现状基线可按 6.8 节在下级辅助文档中展开。
- 沉淀前先检查现有规则是否已经覆盖；优先精确补充或改写原条目，不重复新增近义规范。

### 6.8 下级辅助文档

只有同时满足以下条件时，才可创建下级辅助文档：

- 内容不构成新的开发、测试或上线对象，不需要独立禅道需求。
- 内容与一个已存在需求直接相关，并能明确归属到该需求 README.md。
- 内容需要长期复用，且完整放入 README.md 会明显削弱主 PRD 的聚焦和可读性。
- 用户已经明确确认采用“唯一正式 README.md + 下级辅助文档”的维护方式。

适合的内容包括：完整数据链路、接口字段映射、现状基线、操作说明或其他非 PRD 的长期参考资料。

创建和维护时必须遵守：

- 统一放在对应需求目录的 `docs/` 下，并由父级 README.md 在“关联需求与参考资料”或其他合适位置明确链接和说明用途；父级 README.md 应直接展示辅助文档的完整 GitHub URL，确保复制到钉钉后仍可直接打开。
- 文档开头同时写清自身完整 GitHub URL、归属需求、父级正式 PRD 完整 GitHub URL、辅助文档定位，并明确“正式范围和验收以父级 README.md 为准”。归属需求名称应做成指向对应完整禅道 URL 的可点击链接，方便从钉钉直接进入禅道；这不替代正式需求 README.md 顶部直接展示禅道完整 URL 的要求。
- 不单独复制 PRD 的禅道头部、修改纪要、调研纪要和验收标准，不伪装成第二份需求文档。
- 本期新增或变更的业务范围、需要研发实现的规则或验收口径发生变化时，必须先在父级 README.md 中形成正式结论；辅助文档可以详细记录已确认的现状基线，但不得只修改辅助文档形成隐藏需求。
- 辅助文档可详细展开已确认的数据关系、接口层级和现状链路，但不得与父级 README.md 重复维护两套会独立演进的规则；发生冲突时以父级 README.md 为准。
- 如果资料后来形成独立开发、测试或上线对象，应按新需求规则建立新的唯一 README.md，不再继续作为辅助文档扩张。

## 7. 关联需求与参考资料

需求依赖历史 PRD、原型或业务资料时，按需增加：

```text
## 关联需求与参考资料

- 前置需求：M-xxxxx【需求名称】
- 历史 PRD：完整 GitHub URL
- 参考原型：GitHub 仓库或页面地址
- 影响模块：具体业务端、页面、接口或任务链路
```

需求 README.md 及需要复制到钉钉的下级辅助 Markdown 文档引用仓库内资料时，必须直接展示完整 GitHub URL；只有明确不复制到钉钉、仅在仓库内阅读的内部文档才可使用相对路径。外部原型、禅道、语雀或接口文档使用完整链接。只添加与本次实施直接相关的资料。

引用下级辅助文档时，在父级 README.md 中直接展示完整 GitHub URL，不使用只在 GitHub 仓库内有效的相对链接；同时标明其为辅助文档、用途以及不构成独立需求。下级辅助文档回链父级 README.md 时同样直接展示完整 GitHub URL，确保整篇辅助文档复制到钉钉后仍可打开父级 PRD。

## 8. 常见需求重点

### 8.1 localStorage / 本地缓存

写清：缓存内容、读取时机、写入时机、默认值、账号和业务端隔离、浏览器/设备范围、清缓存表现、历史任务影响及异常兜底。

### 8.2 取数逻辑优化

写清：触发入口、查询参数、数据源、优先级、多条命中规则、无数据处理、单个/批量是否统一和历史逻辑影响。

还应根据实际场景补充：

- 接口返回多条候选且系统需要择一时，分别定义硬排除条件、排序维度、结果有效性门槛和同级兜底规则：硬排除项不得重新进入降级范围；排序维度只决定候选先后；结果有效性门槛决定候选能否作为最终结果；同级兜底只用于稳定候选顺序。写清实际执行顺序，并检查“取第一条”等表述是否与字段缺失或空值时继续降级的规则冲突，不得把结果有效性门槛重复计算为排序权重。
- 下游同时依赖命中记录的多个关联字段时，必须从最终命中的同一个结果对象成对或成组读取，不得假设同一查询返回的多条候选中这些字段必然相同，也不得选中一条记录后再从接口第一条或其他记录拼接字段。定稿前应使用真实多结果响应核对字段值、空值和类型；字段缺失、`null`、空字符串、纯空白及非法值如何跳过、降级或失败必须明确。接口文档只能解释字段含义，抽样中“多数一致”不能作为系统不变量，出现一个反例即应放弃“全部相同”的假设。
- 同一取数能力同时存在单个查询和批量入口时，应分别写清各入口如何取得店铺、账号、站点或其他查询上下文，不得因下游择优规则一致就假设上游参数来源一致；同时明确批量逐行关联、行间数据隔离、单行失败是否影响其他行，以及相同查询条件在不同入口的结果一致性。
- 实时取数需要串联多个接口时，应逐步写清每个接口的请求条件、响应字段、稳定关联键和下游用途，并区分主链路必需数据与可选补充数据。每一步无数据、空值或调用失败时是阻断、留空继续还是由业务人工补齐必须明确；如需研发评估聚合接口、并行调用、超时或缓存，只记录评估目标和必须保持的业务结果，不替研发预设未经确认的技术方案。
- 批量入口串联多个数组或列表型接口时，须分别确认每个接口是否支持多参数、单批上限、超限响应、HTTP 与业务成功状态、无数据项是否返回空占位对象，以及结果稳定关联键；不得仅凭单参数示例、请求体外观或某个接口的能力推断其他接口。当前业务批量小于接口上限时，写清有效参数是否合并为一次调用及无有效参数是否发起空请求；未来扩量只能按各接口自身上限独立评估分片，并通过稳定键聚合，是否允许并发仍按第 8.3 节的真实接口验证安全边界处理。
- 逐行文本批量导入必须写清预处理的固定顺序：拆分行、去除每行首尾空白、丢弃空白行、校验数量上限、按完整字符串精确去重并保留首次出现位置。数量上限是在去重前还是去重后计算、空白行和重复行是否占额度、是否保持前导零和大小写均须分别明确；未确认前不得把 SKU、ID 等字符串转为数字或擅自增加纯数字校验。
- 当任务创建依赖“命中主对象或主键”，但任务完整性还依赖标题、类目、尺寸、图片等补充字段时，必须分开定义主链路门槛和完整性门槛。主对象完全不存在、响应异常与对象存在但字段不完整不得混为同一种失败；单个入口是回填已有内容后阻止保存，还是直接查询失败，以及批量入口是创建待完善任务还是不创建任务，均须独立写清。补充接口失败不得在未经确认时扩大为主链路失败。
- 上游多个字段需要合并到一个可人工编辑的页面控件，并在下游再次组装为请求文本时，应分别定义上游读取顺序、页面展示分隔方式、空值清理、人工编辑后的标准化、最终任务值优先级、下游前缀与分隔符、页面单位与请求单位，以及字数统计口径。自动取数只作为初始值时，应明确最终保存和提交以当前任务值为准；页面计数、保存校验、批量状态判断和最终请求须复用同一标准化和组装结果。
- 在已有普通搜索框中增加新字段时，分别写清新增字段与原有字段的匹配方式；新增 ID 使用精确匹配，不等于可以把店铺、标题、SKU 等原有字段统一改成精确匹配，原有行为是否变化必须明确。
- 查询值由多个落库字段拼接而成时，写清展示或复制格式、源字段、关联关系、整体拼接匹配或拆分匹配口径、重复命中和空值处理。不新增组合落库字段时也要正向说明；未核实原始字段字符范围前，不得默认分隔符一定不会出现在原始值中。
- 同一页面存在多个批量查询组件时，写清各组件的字段、默认值、输入内容、浮层和重置状态是否独立，普通搜索与各批量条件之间的 AND/OR 关系，上限按单组件还是全页合计计算，以及多个组件同时达到上限时的查询和性能验收口径。

### 8.3 接口字段调整

先通过接口文档、实际请求和实际响应还原现状，不得仅根据字段名称猜测业务含义。写清：

- 字段位于请求还是响应、所在层级、名称和类型。
- 字段是现有、新增、修改还是废弃，是否必填及默认值。
- `null`、空字符串、空数组、非法值和多参数同时传入时的处理。
- 接口业务状态成功时，仍须区分“结果字段（如 `data`）为空数组或无当前查询项”与“结果字段缺失、为 `null` 或类型不符合约定”。前者可以是正常无数据，后者属于响应结构异常；两者是否阻断主链路、是否只影响当前行或当前分组、是否继续返回其他可用资料，以及结构异常需要记录什么定位信息均须明确，不得用同一个笼统的“接口失败”覆盖。
- 参数组合对应的业务模式、处理优先级，以及是否允许自动降级或扩大查询范围。
- 现有字段和返回结构的兼容、历史数据处理及下游使用方式。
- 名称相近字段必须明确区分，特别是请求/响应、单数/复数和大小写差异。

对上游或第三方接口新增响应字段、且本期仅补齐解析或存储时，还应写清：

- 默认沿用现有接口调用、轮询、状态判断、落库触发、任务流转和记录关联逻辑；除非业务明确确认，不得顺带新增成功状态门槛、重试机制或状态规则。
- 数组或列表返回按每个结果对象内的稳定标识和字段建立关联；当子项已包含 URL、ID 或业务键时，不得仅依赖外层数组下标强行映射。
- 明确源字段路径、目标表与字段、单条记录粒度、字段类型转换，以及是复用现有字段还是新增字段。
- 区分直接改造链路和业务回归入口：共享后端逻辑是直接改造点，实际使用页面、路由或任务入口应作为回归范围逐一列明。
- 如存在重复轮询或重复处理，明确覆盖规则：空值、非法值、默认值是否可以覆盖已有合法值，以及默认值能否被后续合法值补齐。
- 历史数据处理区分“专项回刷”和“现有流程自然重新处理”；不做专项修复时，明确不新增 SQL、批处理、重新调用接口或扩大查询范围。

请求和响应结构容易产生理解偏差时，应提供精简 JSON 示例，并注明示例对应的查询模式或业务场景。

真实接口验证必须遵守以下安全边界：

- 将接口文档页面访问能力与 API 可达性分开判断。ShowDoc、Swagger 或其他资料页无法打开，不等于 API 不可调用；单次 API 连接失败、超时或重置，也不等于接口永久不可用。分别识别网络异常、HTTP 响应、业务成功状态、业务无数据和响应结构，不混为同一结论。
- 默认仅使用已知正常业务样例，按单次、串行、低频方式验证请求能否成功、业务状态、返回数量、字段层级和稳定关联键。未经用户明确授权，并确认接口负责人或安全测试环境允许，不得进行瞬时并发、压力测试、边界上限探测、最大参数量试探或高频重复请求。
- 遇到临时网络异常时，不得通过增加请求次数、并发数或参数量排查。确需再次确认时，等待链路恢复后使用一个正常业务样例重新验证；如果正常业务调用已经恢复，不再补做并发或边界测试。
- 接口单批上限、并发能力和性能指标优先采用接口负责人、正式文档或已安全确认的既有事实。不得为了给 PRD 补充数字而主动对生产或不明环境做容量探测；未来扩量只记录业务目标、已知约束和研发评估要求，不把未经确认的容量方案写成本期规则。
- PRD 只记录会影响业务规则、研发实现或测试验收的稳定结论。个人网络出口、临时网关限制、偶发连接错误、单次探测耗时和内部排查过程默认不进入正式文档；除非这些信息已经构成需要处理的现网影响、正式性能基线或验收条件。
- 调用和回报过程中只展示完成判断所需的最少信息，不输出真实 Authorization、Token、Cookie、密钥或完整敏感业务数据。请求示例进入仓库前必须再次脱敏。

### 8.4 已上线接口的兼容扩展

接口已被其他系统在线使用时，PRD 必须额外写清：

- 当前调用方、现有请求方式和线上使用场景，不能只描述改造后的新能力。
- 是否允许接口提供方先上线、调用方后续再逐步接入，以及旧调用方无需改造即可继续使用的兼容要求。
- 现有字段由必填改为非必填、新增入参或新增出参时，不得删除、重命名或改变现有字段类型和原有非空场景逻辑。
- 按参数组合识别模式时，写清优先级、空值处理和无结果行为；除非业务明确要求，不得静默自动扩大查询范围。
- 新增响应字段后，应使用当前线上调用方版本完成真实兼容验证，关注严格反序列化、字段校验和前端解析。
- 接口交付前同步调用方负责人，并更新 ShowDoc、Swagger 或其他正式接口文档，补充字段定义、请求/响应示例和参数优先级。
- 引用 CURL 或请求示例时，必须移除或脱敏真实 `Authorization`、Token、Cookie、密钥及其他敏感信息，禁止写入仓库。

### 8.5 线上问题修复

写清：问题表现、影响范围、修复口径、不影响范围、是否补数据和验收样例。修复型需求不要写成重 PRD。

### 8.6 轻量展示补齐与样式优化

写清：实际影响的业务端和全部页面入口、需要对齐的现有页面或组件、展示字段的数据来源、空值和超长处理、是否只改前端展示、共享组件的回归范围，以及原有操作不受影响的验收要求。

只是在现有页面补充已有字段、对齐另一业务端的既有展示，或调整局部间距、换行和布局时，通常不需要单独制作 HTML 原型；如涉及新的信息层级、复杂交互或整体页面改版，则应单独建立原型仓库。

## 9. 索引维护

只维护根目录 README.md 中的需求索引。

根目录需求索引只链接每个需求唯一正式的 README.md，不单独索引 `docs/` 下的辅助文档。

新建需求时补充：日期、需求编号、业务端、需求名称、需求类型和 PRD 链接。

索引按 PRD 正式创建或落库日期倒序排列；索引日期应与该需求首次 `V1.0` 的修改纪要日期一致。跨日期协作时按实际落库日期重新核对，不沿用聊天开始日期或上一自然日。

开发、测试和上线状态以禅道为准，不在 GitHub 重复维护。更新已有需求名称或路径时，修正原索引行，不重复新增。

索引中的“业务端”按整份 PRD 的实际影响范围填写，不只照抄需求标题中的业务端标签；同一需求实际同时影响美工版和运营版时，填写“美工+运营”。

业务端建议：`运营`、`美工`、`美工+运营`、`公共`。

需求类型建议：`配置`、`接口`、`规则`、`修复`、`缓存`、`数据`、`其他`。

## 10. 禁止事项

- 不得为同一需求维护两份 PRD。
- 不得把下级辅助文档写成第二份 PRD，或只在辅助文档中新增本期正式范围、需要研发实现的业务规则和验收口径。
- 不得在未获用户明确确认时默认创建 `docs/` 或辅助文档，也不得将辅助文档直接散放在需求 README.md 旁边。
- 不得创建月份 README 或第二层索引。
- 不得额外创建重复模板文档。
- 不得为了套模板填写无意义章节。
- 不得擅自脑补业务规则、接口字段或验收口径。
- 不得把内部讨论、历史文档纰漏、纠错过程或从未发生的错误实现写入正式 PRD，除非其已实际构成线上影响或兼容风险。
- 不得修改历史需求的已确认口径，除非用户明确要求。
- 不得将明显需要 HTML 原型的需求强行放入本仓库。
- 不得用 GitHub 索引重复维护禅道中的开发、测试和上线状态。
- 不得将 PRD 顶部的禅道需求和 GitHub PRD 写成隐藏完整地址的 Markdown 链接。
- 不得在仓库文档中保留真实 Authorization、Token、Cookie、密钥或其他敏感信息。
- 未取得正式需求名称和禅道信息前，不得默认创建 `TEMP` 目录。
- 未取得明确 GitHub 发布确认前，不得提交、推送、创建 PR 或合并；“可以落地”本身不视为发布授权。
